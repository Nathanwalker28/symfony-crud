{% extends "base.html.twig" %}


{% block body %}
	<div class="container">
        <div class="row mt-5">
            {{  form_start(form) }}
            {{ form_widget(form) }}
            <button type="submit" class="btn btn-primary">enregistrer les modifications</button>
		    {{  form_end(form) }}
        </div>
	</div>
{% endblock %}
